import NeonRoom from "@/components/lobby/NeonRoom";

/** Aula Virtual — misma lógica de controles que el lobby; estética de aula escolar. */
export default function AulaVirtualRoom() {
  return <NeonRoom variant="aula-virtual" />;
}
