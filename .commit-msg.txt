Reusar MirrorSbsRoot existente para el modo 2D del lobby

- Borrar Mirror2DRenderer.tsx y toda la maquinaria mirror2D que metimos
  en NeonRoom (doble columnas en HoloScreen/ForcedFloatingVideoScreen,
  hudClipStyle, props mirror2D, scissor split). Era complejo y no
  producia el espejo exacto que pedia el usuario.
- main.tsx: exponer SBS_MIRROR_EVENT como evento global. MirrorSbsRoot
  pasa de useState fijo a useState + listener de window. Cualquier
  vista puede activar el SBS espejo con
    window.dispatchEvent(new CustomEvent(SBS_MIRROR_EVENT, {detail:true}))
- NeonRoom: el boton "2D" ahora solo dispara ese evento. Toda la
  captura DOM con html2canvas y el render en dos canvases ojo izq/der
  ya estaba implementada en MirrorSbsRoot (misma tecnica que funciono
  con el video de Bele); el lobby la reutiliza tal cual.
- Cleanup al desmontar el lobby: apagamos el SBS por si el usuario
  cambia de ruta con el modo activo.
